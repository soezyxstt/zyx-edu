# Task P3-03: Deterministic Learner Profile Construction

## Context
The grounded tutor must know the learner and adapt explanations based on the student's weak concepts.

## Objectives
- Create `lib/learner-profile.ts`.
- Fetch data from P1A concept mastery tables and `tutor_session_summaries`.
- Construct a profile payload: `{weakConcepts: string[], masteredConcepts: string[], recentStruggles: string[], recentTutorTopics: string[]}`.
- Ensure profile construction is 100% deterministic (zero AI/LLM calls).

## Verification
- Verify the SQL queries return correctly matching the user's progress.
