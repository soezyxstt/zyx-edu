# P0: Infrastructure Activation and Baseline

Goal: every service provisioned, keys in env, 100% of Gemini traffic visible in AI Gateway, baseline numbers recorded. No product features.

Flag: none (config only). Depends on: nothing. Read `globals.md` first.

## Progress
- [ ] Read-before-coding done
- [ ] Env vars added
- [ ] Out of code done
- [ ] Deliverables done
- [ ] Gate green

## Read before coding
| File | Why |
|---|---|
| `lib/env.ts` | All env keys live here; you add 3 |
| `lib/gemini.ts` | Gateway routing already coded; understand the `CF_AI_GATEWAY_URL` branch |
| `lib/db/index.ts` | Confirm Turso wiring |
| `lib/pinecone.ts` | Confirm index name usage |

## Files to create
| File | Purpose |
|---|---|
| `docs/baselines.md` | Table of baseline metrics, updated monthly |
| `scripts/r2-smoke.ts` | One-off R2 put/get/delete round trip. Reuse `lib/storage/r2.ts` (already exists), do not write a new client |

## Files to edit
| File | Change |
|---|---|
| `lib/env.ts` | Add optional `CF_ACCOUNT_ID`, `CF_KV_NAMESPACE_ID`, `CF_API_TOKEN` to schema and runtime map |

## Do not touch
`lib/gemini.ts` logic, `db/schema.ts`, anything in `app/`.

## Out of code (exact clicks)
- [ ] Cloudflare account at dash.cloudflare.com.
- [ ] AI Gateway: AI > AI Gateway > Create Gateway, name `zyx-edu`. Provider URL format `https://gateway.ai.cloudflare.com/v1/{account_id}/zyx-edu/google-ai-studio`. Put in `.env` and Vercel as `CF_AI_GATEWAY_URL`. Logging ON, caching OFF (P3 enables it).
- [ ] KV: Storage & Databases > KV > Create namespace `zyx-ai-cache`. Record namespace ID.
- [ ] R2: ALREADY PROVISIONED (bucket `zyx`, public `pub-*.r2.dev` URL, migration done 2026-06-10, see `plans/p7.md`). Only verify all `R2_*` env vars are present locally and in Vercel.
- [ ] API token: My Profile > API Tokens > Create, permission `Workers KV Storage: Edit`. Record `CF_ACCOUNT_ID`, `CF_KV_NAMESPACE_ID`, `CF_API_TOKEN`.
- [ ] Turso: confirm `TURSO_CONNECTION_URL` + `TURSO_AUTH_TOKEN` in Vercel; if missing run `turso db create zyx-edu` and `turso db tokens create zyx-edu`.
- [ ] Pinecone: confirm `PINECONE_API_KEY`, `PINECONE_INDEX_NAME` (768 dims, cosine).
- [ ] `wrangler login` once (needed only for gated P8/P9).
- [ ] After 48h of normal use, fill `docs/baselines.md`: dashboard p75 load (Vercel analytics), Gemini req/day (Gateway), Turso row reads/day, Pinecone record count.

## Deliverables
- [ ] `docs/baselines.md` committed with 4 filled numbers.
- [ ] All env vars present locally and in Vercel.
- [ ] `scripts/r2-smoke.ts` runs clean.

## Gate
| Done | # | Test | Steps | Pass when |
|---|---|---|---|---|
| [ ] | 0.1 | Gateway proxying | Ask AI tutor 5 questions, run 1 generation job | All requests appear in Gateway logs |
| [ ] | 0.2 | Prod DB | Deploy preview, open `/dashboard` | Page loads, no DB errors in Vercel logs |
| [ ] | 0.3 | R2 round trip | `npx tsx scripts/r2-smoke.ts` | put, get (bytes equal), delete all succeed |
| [ ] | 0.4 | Build health | `npm run lint && npm run build` | Zero new errors |
