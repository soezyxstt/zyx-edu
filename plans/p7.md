# P7: Storage on R2 (migration ALREADY DONE, teardown remains)

Status correction (verified in code 2026-06-12): the UploadThing to R2 migration was completed on 2026-06-10 by a prior agent. `lib/storage/index.ts` is a working provider abstraction (`STORAGE_PROVIDER_MODE=r2|uploadthing`, default r2) already used by diktats (`lib/diktat-actions.ts`, `app/admin/ai/diktats/*`), admin files (`app/admin/files/*`), and `app/api/storage/upload/route.ts`. All 3 production files were migrated with checksum verification. Bucket: `zyx` (not `zyx-edu-assets`), public URL `https://pub-f065457c17ae4c49a9a6321465367cd4.r2.dev`.

What remains is monitoring, UploadThing teardown, and recreating the helper scripts that were deleted with the old `scripts/` directory.

Flag: `STORAGE_PROVIDER_MODE` (exists, default r2). Depends on: P0. Read `globals.md` first.

## Progress
- [x] Storage adapter (`lib/storage/index.ts` + `r2.ts` + `uploadthing-provider.ts`)
- [x] Diktats, admin files, upload route on R2
- [x] Backfill of existing files (3/3, checksums verified, 2026-06-10)
- [ ] Helper scripts recreated
- [ ] Monitoring period passed (2 weeks stable on prod)
- [ ] UploadThing files deleted
- [ ] Optional teardown decision recorded
- [ ] Gate green

## Read before coding
| File | Why |
|---|---|
| `lib/storage/index.ts` | The existing provider interface; extend, never replace |
| `lib/storage/r2.ts`, `lib/storage/uploadthing-provider.ts` | Both implementations |
| `app/api/storage/upload/route.ts` | Existing upload path |
| `lib/diktat-actions.ts`, `app/admin/files/actions.ts` | Integrated call-sites |
| `db/schema.ts` | `drive_item.uploadthingKey` and `diktats.fileUrl` now hold R2 keys (legacy names, rename optional and low priority) |

## Files to create (recreate, the old `scripts/` dir was deleted)
| File | Purpose |
|---|---|
| `scripts/verify-r2.ts` | Health check: every DB file key exists in R2, size > 0, HTTP 200 via public URL. Print pass/fail table |
| `scripts/check-file-links.ts` | HTTP 200 check over every file URL in DB (provider-agnostic) |
| `scripts/delete-uploadthing-files.ts` | Deletes the 3 legacy UT files AFTER verify passes. Keys: `66nPsBvCMf37MOdARFLylNEfxFIKRg8PX0p76TotknUDe2aV`, `66nPsBvCMf37FCH5kyB2zTY8muXxAHU13dO9Btjf5c4bWhrQ`, `66nPsBvCMf37EAw8SnJ0BsNZAPt5C8jYfiehG2MrySJmR4V6` (via UTAPI `DELETE /v6/files`) |

## Do not touch
`lib/storage/index.ts` interface, `lib/storage/uploadthing-provider.ts` (keep until teardown decision), `UPLOADTHING_TOKEN` env (keep until teardown), the repurposed key columns (rename is optional, skip).

## Remaining work
1. Recreate the 3 scripts above.
2. Confirm production env has: `STORAGE_PROVIDER_MODE=r2`, all `R2_*` vars (bucket `zyx`).
3. Monitoring (2 weeks on prod): file previews in `/admin/files`, diktat PDF downloads, weekly `npx tsx scripts/verify-r2.ts`.
4. After 2 weeks stable: run `delete-uploadthing-files.ts`.
5. After 1 month stable, decide and record in `docs/baselines.md`: remove `lib/storage/uploadthing-provider.ts`, `UPLOADTHING_TOKEN`, `@uploadthing/react` + `uploadthing` packages. Before removing, run: `grep -r "uploadthing|UploadThing|utfs.io|ufs.sh"` over `*.ts, *.tsx` and resolve every hit.

## Out of code
- [ ] Confirm R2 bucket CORS allows GET/PUT from app origins.
- [ ] Decide public URL: keep `pub-*.r2.dev` or attach `assets.{domain}` (then update `R2_PUBLIC_URL`).
- [ ] After teardown: downgrade/abandon UploadThing account.
- [ ] Check R2 dashboard monthly: storage GB + Class A ops vs free tier (10 GB).

## Gate
| Done | # | Test | Steps | Pass when |
|---|---|---|---|---|
| [ ] | 7.1 | New asset | Compile a diktat | URL serves from R2, renders in viewer |
| [ ] | 7.2 | Health | `npx tsx scripts/verify-r2.ts` | All keys exist, all 200 |
| [ ] | 7.3 | Upload | 10 MB PDF via `/admin/files` | Succeeds, preview loads from R2 |
| [ ] | 7.4 | Rollback | `STORAGE_PROVIDER_MODE=uploadthing` in preview | New uploads route via UT (provider still wired) |
| [ ] | 7.5 | Teardown safety | Run UT deletion only after 7.2 green + 2-week monitor | Legacy previews still load (they read R2 keys) |
